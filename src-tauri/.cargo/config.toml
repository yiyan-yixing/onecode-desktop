# 构建环境修复：~/.local/bin/cc 被错误 symlink 到 claude 二进制
# （2026-08-21 创建），导致 cargo 链接报 "unknown option '-lSystem'"。
# 这里显式固定 Apple clang 作为编译器/链接器，不依赖用户 PATH 里的 cc。
[build]
rustflags = ["-C", "linker=/usr/bin/clang"]

[env]
CC = "/usr/bin/clang"
CXX = "/usr/bin/clang++"
